# Database (Supabase) — connection string Postgres
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME

# better-auth
BETTER_AUTH_SECRET=generate-with: openssl rand -base64 32
BETTER_AUTH_URL=http://localhost:3000
